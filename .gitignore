# secrets
.env
web/.env

# python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# wren memory index (rebuilt on first run)
.wren/

# data (too large for git — share separately)
data/data.duckdb
*.duckdb
*.csv
uploads/

# streamlit secrets (local dev only — set in Streamlit Cloud dashboard)
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db
